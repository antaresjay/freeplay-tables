[game]
name = "Homura Hime"
exe = "homurahime.exe"
notes = "Converted from a Cheat Engine table by ndck1. Source: https://fearlessrevolution.com/viewtopic.php?t=38445"
verified = []
author = "ndck1"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "one-hit-kill-ctrl-numpad-1"
name = "One Hit Kill (Ctrl + Numpad 1)"
category = "combat"
description = ""
hint = ""
type = "script"
source = """
// Game Executable   : HomuraHime.exe
// Author            : ndck76
// Executable Version: 6000.0.28.13996
// Game Version      : Build.22196953
// Script Date       : 2026-03-05


define(address,EnemyHUDContorller:Update+97)
define(bytes,F3 0F 10 81 A8 00 00 00 F3 0F 5A C0 48 8B 4E 40)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)

label(code)
label(return)

newmem:
  mov [rcx+000000A8],(float)1

code:
  movss xmm0,[rcx+000000A8]
  cvtss2sd xmm0,xmm0
  mov rcx,[rsi+40]
  jmp return

address:
  jmp far newmem
  nop 2
return:

[DISABLE]

address:
  db bytes
  // movss xmm0,[rcx+000000A8]
  // cvtss2sd xmm0,xmm0
  // mov rcx,[rsi+40]

dealloc(newmem)"""
