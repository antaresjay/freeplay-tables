[game]
name = "BioShock Infinite FLT"
exe = "bioshockinfinite.exe"
notes = "Converted from a Cheat Engine table by gir489. Source: https://fearlessrevolution.com/viewtopic.php?t=3905"
verified = []
author = "gir489"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "infinite-ammo"
name = "Infinite Ammo"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
aobscan(function,50 8B CD E8 ?? ?? ?? ?? 8B 4C 24 54)

[ENABLE]
alloc(newmem,512)
alloc(shotgun,64)
alloc(smg,64)
alloc(rpg,64)
alloc(rifle,64)
alloc(original,64)

function+C:
jmp newmem
nop

original:
add [ebp+08],ecx
mov eax,[ebp+08]
jmp function+12

newmem:
cmp ecx,0xFFFFFFFF
jne original
cmp [ebp],0x142 //1887
je shotgun
cmp [ebp],0x1C1 //Type 100
je smg
cmp [ebp],0x1E4 //Ruger
je smg
cmp [ebp],0x124 //M1
je rifle
cmp [ebp],0x140 //Snapar rafle
je shotgun
cmp [ebp],0x1ED //Crank Gun
je smg
cmp [ebp],0xED //Launcher
je rpg
cmp [ebp],0x79 //Volly Gun
je shotgun
cmp [ebp],0x1E7 //Colt Navy
je shotgun
cmp [ebp],0x1F6 //Repeater
je rifle
cmp [ebp],0x248 //Burst rifle
je smg
cmp [ebp],0x58 //Heater
je rpg
cmp [ebp],0x98 //Hailfire
je shotgun
jmp original

shotgun:
mov [ebp+8],0xF
jmp original

rpg:
mov [ebp+8],0x8
jmp original

smg:
mov [ebp+8],0x65
jmp original

rifle:
mov [ebp+8],0x3C
jmp original

[DISABLE]
dealloc(newmem)
dealloc(shotgun)
dealloc(smg)
dealloc(rpg)
dealloc(rifle)
dealloc(original)

function+C:
add [ebp+08],ecx
mov eax,[ebp+08]"""
