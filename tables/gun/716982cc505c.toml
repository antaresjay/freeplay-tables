[game]
name = "Gun"
exe = "gun.exe"
notes = "Converted from a Cheat Engine table by 1lvl1. Source: https://fearlessrevolution.com/viewtopic.php?t=23312"
verified = []
author = "1lvl1"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "hp"
name = "hp"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(hp,Gun.exe,D9 40 30 D8 1D 08 F6 68 00 DF E0 F6 C4 41 0F) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  fld dword ptr [eax+30]
  mov [eax+30],(float)900
  fcomp dword ptr [Gun.exe+28F608]
  jmp return

hp:
  jmp newmem
  nop 4
return:
registersymbol(hp)

[DISABLE]

hp:
  db D9 40 30 D8 1D 08 F6 68 00

unregistersymbol(hp)
dealloc(newmem)"""

[[cheat]]
id = "horse"
name = "horse"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(horse,Gun.exe,8B 48 28 8B 46 44) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [eax+28],(float)900
  mov ecx,[eax+28]
  mov eax,[esi+44]
  jmp return

horse:
  jmp newmem
  nop
return:
registersymbol(horse)

[DISABLE]

horse:
  db 8B 48 28 8B 46 44

unregistersymbol(horse)
dealloc(newmem)"""

[[cheat]]
id = "ammo"
name = "ammo"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(ammo,Gun.exe,48 5E 89 45 04) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  inc eax
  pop esi
  mov [ebp+04],eax
  jmp return

ammo:
  jmp newmem
return:
registersymbol(ammo)

[DISABLE]

ammo:
  db 48 5E 89 45 04

unregistersymbol(ammo)
dealloc(newmem)"""

[[cheat]]
id = "throw"
name = "throw"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(throw,Gun.exe,3B F8 7E 02 8B F8) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  //cmp edi,eax
  jle Gun.exe+15AD2D
  mov edi,eax
  jmp return

throw:
  jmp newmem
  nop
return:
registersymbol(throw)

[DISABLE]

throw:
  db 3B F8 7E 02 8B F8

unregistersymbol(throw)
dealloc(newmem)"""

[[cheat]]
id = "rapid"
name = "rapid"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(rapid,Gun.exe,8D 7E 40 D9 1F) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  //lea edi,[esi+40]
  fstp dword ptr [edi]
  jmp return

rapid:
  jmp newmem
return:
registersymbol(rapid)

[DISABLE]

rapid:
  db 8D 7E 40 D9 1F

unregistersymbol(rapid)
dealloc(newmem)"""

[[cheat]]
id = "slow"
name = "slow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(slow,Gun.exe,D8 6E 1C D9 5E 1C) // should be unique
aobscanmodule(slowmo,Gun.exe,D9 46 1C D8 1D 10 CC 67 00) // should be unique

alloc(slowmem,$1000)
alloc(slowmomem,$1000)

label(slowcode)
label(slowreturn)

label(slowmocode)
label(slowmoreturn)

slowmem:

slowcode:
  mov [esi+1C],(float)1
  fsubr dword ptr [esi+1C]
  fstp dword ptr [esi+1C]
  jmp slowreturn

slow:
  jmp slowmem
  nop
slowreturn:
registersymbol(slow)

slowmomem:

slowmocode:
  mov [esi+1C],(float)1
  fld dword ptr [esi+1C]
  fcomp dword ptr [Gun.exe+27CC10]
  jmp slowmoreturn

slowmo:
  jmp slowmomem
  nop 4
slowmoreturn:
registersymbol(slowmo)

[DISABLE]

slow:
  db D8 6E 1C D9 5E 1C

slowmo:
  db D9 46 1C D8 1D 10 CC 67 00

unregistersymbol(slow)
unregistersymbol(slowmo)
dealloc(slowmem)
dealloc(slowmoem)"""

[[cheat]]
id = "flask"
name = "flask"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(flask,Gun.exe,89 46 20 E8 42 FC FF FF) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [esi+20],6
  call Gun.exe+14A900
  jmp return

flask:
  jmp newmem
  nop 3
return:
registersymbol(flask)

[DISABLE]

flask:
  db 89 46 20 E8 42 FC FF FF

unregistersymbol(flask)
dealloc(newmem)"""

[[cheat]]
id = "movement"
name = "movement"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(boost,Gun.exe,89 8E 1C 08 00 00) // should be unique
aobscanmodule(speed,Gun.exe,D9 5C 24 04 D9 86 C8 00 00 00) // should be unique
aobscanmodule(jump,Gun.exe,D9 9F CC 00 00 00 8B 46) // should be unique

alloc(boostmem,$1000)
alloc(speedmem,$1000)
alloc(jumpmem,$1000)

alloc(smult,4)
alloc(jmult,4)
registersymbol(smult)
registersymbol(jmult)

label(boostcode)
label(speedcode)
label(jumpcode)
label(boostreturn)
label(speedreturn)
label(jumpreturn)

boostmem:

boostcode:
  mov [esi+0000081C],(float)500
  jmp boostreturn

boost:
  jmp boostmem
  nop
boostreturn:
registersymbol(boost)

speedmem:

speedcode:
  fmul dword ptr [smult]
  fstp dword ptr [esi+000000C8]
  jmp speedreturn

smult:
  dd (float)5

speed:
  jmp speedmem
  nop 5
speedreturn:
registersymbol(speed)

jumpmem:

jumpcode:
  fmul dword ptr [jmult]
  fstp dword ptr [edi+000000CC]
  jmp jumpreturn

jmult:
  dd (float)1.5

jump:
  jmp jumpmem
  nop
jumpreturn:
registersymbol(jump)

[DISABLE]

boost:
  db 89 8E 1C 08 00 00

speed:
  db D9 5C 24 04 D9 86 C8 00 00 00

jump:
  db D9 9F CC 00 00 00

unregistersymbol(boost)
unregistersymbol(speed)
unregistersymbol(jump)
unregistersymbol(smult)
unregistersymbol(jmult)
dealloc(boostmem)
dealloc(speedmem)
dealloc(jumpmem)
dealloc(smult)
dealloc(jmult)"""

[[cheat]]
id = "speed"
name = "speed"
category = "movement"
description = ""
hint = ""
type = "value"
value_type = "f32"

[cheat.locator]
find = "symbol"
symbol = "smult"
hops = []

[[cheat]]
id = "jump"
name = "jump"
category = "movement"
description = ""
hint = ""
type = "value"
value_type = "f32"

[cheat.locator]
find = "symbol"
symbol = "jmult"
hops = []
