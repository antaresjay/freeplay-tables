[game]
name = "OUTRIDERS table v: 1.0 CT"
exe = "outriders-win64-shipping.exe"
notes = "Converted from a Cheat Engine table by ModZero. Source: https://fearlessrevolution.com/viewtopic.php?t=30802"
verified = []
author = "ModZero"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "no-reload"
name = "No Reload"
category = "combat"
description = ""
hint = ""
type = "script"
source = """
//Send@ModEngine
[ENABLE]

aobscanmodule(noreload,OUTRIDERS-Win64-Shipping.exe,89 83 10 1A 00 00)
alloc(newmem,$1000,noreload)

label(code)
label(return)

newmem:

code:
  //mov [rbx+00001A10],eax
  jmp return

noreload:
  jmp newmem
  nop
return:
registersymbol(noreload)

[DISABLE]

noreload:
  db 89 83 10 1A 00 00

unregistersymbol(noreload)
dealloc(newmem)"""

[[cheat]]
id = "instant-reload"
name = "Instant Reload"
category = "combat"
description = ""
hint = ""
type = "script"
source = """
//Send@ModEngine
[ENABLE]

aobscanmodule(reloadspeed,OUTRIDERS-Win64-Shipping.exe,DB 74 14 48 8B 03 48 8B CB FF 90)
alloc(newmem,$1000,reloadspeed)

label(code)
label(return)

newmem:
push rbx
mov rbx,[rbx+350] //Pawn
test rbx,rbx
je @f
mov rbx,[rbx+0A08] //CurrentWeapon
mov [rbx+0464],(float)100
mov [rbx+0468],(float)0

@@:
pop rbx

code:
  mov rax,[rbx]
  mov rcx,rbx
  jmp return

reloadspeed+03:
  jmp newmem
  nop
return:
registersymbol(reloadspeed)

[DISABLE]

reloadspeed+03:
  db 48 8B 03 48 8B CB

unregistersymbol(reloadspeed)
dealloc(newmem)"""
