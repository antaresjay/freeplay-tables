[game]
name = "Shape of Dreams: Prologue"
exe = "dreams.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=32668"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "grab-hero-data"
name = "Grab Hero Data"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@Sintrix
define(address,Hero:FrameUpdate+17)
define(bytes,48 8B F1 48 8D 6D 00)

[ENABLE]


assert(address,bytes)
alloc(newmem,$1000,Hero:FrameUpdate+17)
registersymbol(hero)
label(code return hero)

newmem:
mov [hero],rcx

code:
  mov rsi,rcx
  lea rbp,[rbp+00]
  jmp return

hero:
dq 0

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes


unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "increased-dream-dust-gain"
name = "Increased Dream Dust Gain"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,Pickup_DreamDust:OnPickup+23)
define(bytes,48 8B CF 48 8B 55 B8)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,Pickup_DreamDust:OnPickup+23)

label(code)
label(return)

newmem:
mov [rdi+238],#1000

code:
  mov rcx,rdi
  mov rdx,[rbp-48]
  jmp return

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes

dealloc(*)"""

[[cheat]]
id = "increased-gold-gain"
name = "Increased Gold Gain"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,Pickup_BaseGoldOrb:OnPickup+1b)
define(bytes,48 8B F9 48 89 55 A8)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,Pickup_BaseGoldOrb:OnPickup+1b)

label(code)
label(return)

newmem:
mov [rcx+239],#1000

code:
  mov rdi,rcx
  mov [rbp-58],rdx
  jmp return

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes

dealloc(*)"""

[[cheat]]
id = "increased-experience-gain"
name = "Increased Experience Gain"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,Pickup_BaseExpOrb:OnPickup+c)
define(bytes,48 8B F9 48 89 55 D0)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,Pickup_BaseExpOrb:OnPickup+c)
label(code return)

newmem:
mov [rcx+230],(float)500
code:
  mov rdi,rcx
  mov [rbp-30],rdx
  jmp return

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""
