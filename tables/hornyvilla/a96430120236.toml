[game]
name = "Horny Villa"
exe = "hornyvilla.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=32639"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "promotions-cost-zero-coins"
name = "Promotions Cost Zero Coins"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
[ENABLE]

aobscanregion(promotioncost,StripClub.Model.Cards.Promote:get_LevelUpCost,StripClub.Model.Cards.Promote:get_LevelUpCost+88,20 00 00 00 8B 08 89 4D F8)
alloc(newmem,$1000)
label(code return)

newmem:
mov [ecx+20],#0

code:
  mov ecx,[eax]
  mov [ebp-08],ecx
  jmp return

promotioncost+04:
  jmp newmem
return:
registersymbol(promotioncost)

[DISABLE]

promotioncost+04:
  db 8B 08 89 4D F8

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "alternative-promotions-cost-zero-coins"
name = "(Alternative) Promotions Cost Zero Coins"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,StripClub.Model.Cards.Promote:get_LevelUpCost)
define(bytes,55 8B EC 83 EC 18)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)
label(code return)

newmem:
mov [ecx+20],#0
code:
  push ebp
  mov ebp,esp
  sub esp,18
  jmp return

address:
  jmp newmem
  nop
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "complete-event-requirements"
name = "Complete Event Requirements"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@Sintrix|FearlessRevolution
define(address,GreenT.HornyScapes.Tasks.OneWayMergeItemObjective:get_IsComplete+d)
define(bytes,8B 40 14 8B 4F 0C)

define(address2,GreenT.HornyScapes.Tasks.OneWayMergeItemObjective:GetProgress+c)
define(bytes2,8B 40 14 C9 C3)

define(address3,GreenT.HornyScapes.Tasks.SavableObjectiveData+ObjectiveCountableDataMemento:.ctor+1b)
define(bytes3,8B 48 14 8B 45 08)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)

label(code)
label(return)

newmem:
mov [eax+14],#9999

code:
  mov eax,[eax+14]
  mov ecx,[edi+0C]
  jmp return

address:
  jmp newmem
  nop
return:

assert(address2,bytes2)
alloc(newmem2,$1000)

label(code2)
label(return2)

newmem2:
mov [eax+14],#9999

code2:
  mov eax,[eax+14]
  leave 
  ret 
  jmp return2

address2:
  jmp newmem2
return2:

assert(address3,bytes3)
alloc(newmem3,$1000)

label(code3)
label(return3)

newmem3:
mov [eax+14],#9999

code3:
  mov ecx,[eax+14]
  mov eax,[ebp+08]
  jmp return3

address3:
  jmp newmem3
  nop
return3:

[DISABLE]

address:
  db bytes

address2:
  db bytes2

address3:
  db bytes3

dealloc(*)"""

[[cheat]]
id = "max-battlepass-free-tier-level-on-experience-gain"
name = "Max Battlepass Free Tier Level (On Experience Gain)"
category = "game"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

//Send@FearlessRevolution|Sintrix

aobscanregion(bpfreetier,GreenT.HornyScapes.BattlePassSpace.Data.BattlePasLevelInfoCase:AddPoints,GreenT.HornyScapes.BattlePassSpace.Data.BattlePasLevelInfoCase:AddPoints+2c,8B 49 10 03 4D 0C 89 4C 24 04 89 04 24 39 00 90 E8 *)
alloc(newmem,$1000)
label(code return)

newmem:
mov [ecx+10],#50000

code:
  mov ecx,[ecx+10]
  add ecx,[ebp+0C]
  jmp return

bpfreetier:
  jmp newmem
  nop
return:
registersymbol(bpfreetier)

[DISABLE]

bpfreetier:
  db 8B 49 10 03 4D 0C

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "instant-complete-item-merge"
name = "Instant Complete Item Merge"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

//Send@FearlessRevolution|Sintrix

aobscanregion(mergecount,GreenT.HornyScapes.Tasks.MergeItemObjective:get_IsComplete,GreenT.HornyScapes.Tasks.MergeItemObjective:get_IsComplete+32,8B 49 08 3B C1)
alloc(newmem,$1000)

label(code)
label(return)

newmem:
mov [ecx+08],#-1
code:
  mov ecx,[ecx+08]
  cmp eax,ecx
  jmp return

mergecount:
  jmp newmem
return:
registersymbol(mergecount)

[DISABLE]

mergecount:
  db 8B 49 08 3B C1

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "grab-adventure-data"
name = "Grab Adventure Data"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix

[ENABLE]

aobscanregion(advwallet,StripClub.NewEvent.Model.EventWallet:TryAdd,StripClub.NewEvent.Model.EventWallet:TryAdd+5e,8B 49 10 03 4D 10 89 4C 24 04 89 04 24 39 00 8B C0 E8 *)
alloc(newmem,$1000)
registersymbol(event)
label(code return event)

newmem:
mov [event],ecx

code:
  mov ecx,[ecx+10]
  add ecx,[ebp+10]
  jmp return

event:
dq 0

advwallet:
  jmp newmem
  nop
return:
registersymbol(advwallet)

[DISABLE]

advwallet:
  db 8B 49 10 03 4D 10

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "grab-currencies-method-1-purchase-any-lootbox"
name = "Grab Currencies Method 1 (Purchase Any Lootbox)"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

//Send@FearlessRevolution|Sintrix

aobscanregion(cm1,StripClub.UI.Rewards.RewardsWindow:ShowResourcesWindow,StripClub.UI.Rewards.RewardsWindow:ShowResourcesWindow+27,08 8B 87 A8 00 00 00 C7)
alloc(newmem,$1000)
registersymbol(cptr)
label(code return cptr)

newmem:
mov [cptr],esi
code:
  mov eax,[edi+000000A8]
  jmp return

cptr:
dq 0

cm1+01:
  jmp newmem
  nop
return:
registersymbol(cm1)

[DISABLE]

cm1+01:
  db 8B 87 A8 00 00 00

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "grab-energy-purchase-any-lootbox"
name = "Grab Energy (Purchase Any Lootbox)"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

//Send@FearlessRevolution&Sintrix
//CurrencyAmplitudeAnalytics


aobscanregion(en,GreenT.HornyScapes.CurrencyProcessor:GetCount,GreenT.HornyScapes.CurrencyProcessor:GetCount+32,8B 45 0C 89 44 24 04 8B 45 08 89 04 24 8D 6D 00 E8 * 00 00 00 85)
alloc(newmem,$1000)
registersymbol(pbasic)
label(code return pbasic)

newmem:
mov [pbasic],ebx

code:
  mov eax,[ebp+0C]
  mov [esp+04],eax
  jmp return

pbasic:
dq 0

en:
  jmp newmem
  nop 2
return:
registersymbol(en)

[DISABLE]

en:
  db 8B 45 0C 89 44 24 04

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "grab-currencies-method-2-open-story-menu-spend-star"
name = "Grab Currencies Method 2 (Open Story Menu & Spend Star)"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix

[ENABLE]


aobscanregion(cm2,GreenT.HornyScapes.StarShop.UI.StarShopView:UpdateButtonState,GreenT.HornyScapes.StarShop.UI.StarShopView:UpdateButtonState+41,89 3C 24 8B 07 BA * * * * FF 50 BC)
alloc(newmem,$1000)
registersymbol(cptr)
label(code return cptr)

newmem:
mov [cptr],esi


code:
  mov [esp],edi
  mov eax,[edi]
  jmp return

cptr:
dq 0

cm2:
  jmp newmem
return:
registersymbol(cm2)

[DISABLE]

cm2:
  db 89 3C 24 8B 07

unregistersymbol(*)
dealloc(*)"""
