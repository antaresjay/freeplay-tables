[game]
name = "Lost Light"
exe = "game_x64r.exe"
notes = "Converted from a Cheat Engine table by sean24. Source: https://fearlessrevolution.com/viewtopic.php?t=30603"
verified = []
author = "sean24"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "noclip"
name = "NoClip"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
{ Game   : Game_x64r.exe
  Version: 1.1
  Date   : 2024-10-18
  Author : OScheats
  hotkey this only tern on for 1-2 sec whyle jumping into doors/walls
  if no gravity just tern on and walk into anything
}

[ENABLE]

aobscanmodule(INJECT,Game_x64r.exe,F2 0F 5C 4B 1C 4C) // should be unique
alloc(newmem,$1000,INJECT)

label(code)
label(return)

newmem:

code:
  db 90 90 90 90 90
  jmp return

INJECT:
  jmp newmem
return:
registersymbol(INJECT)

[DISABLE]

INJECT:
  db F2 0F 5C 4B 1C

unregistersymbol(INJECT)
dealloc(newmem)"""

[[cheat]]
id = "no-gravity"
name = "No Gravity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
{ Game   : Game_x64r.exe
  Version: 
  Date   : 2024-10-22
  Author : seant

  walk on air, hotkey to tern on and off!! for when leaveing battle.
}

[ENABLE]

aobscanmodule(INJECT,Game_x64r.exe,F3 0F 11 61 04 F3 0F 58 71) // should be unique
alloc(newmem,$1000,INJECT)

label(code)
label(return)

newmem:

code:
  db 90 90 90 90 90
  jmp return

INJECT:
  jmp newmem
return:
registersymbol(INJECT)

[DISABLE]

INJECT:
  db F3 0F 11 61 04

unregistersymbol(INJECT)
dealloc(newmem)"""

[[cheat]]
id = "z"
name = "Z"
category = "misc"
description = ""
hint = ""
type = "value"
value_type = "f32"

[cheat.locator]
find = "static"
module = "Game_x64r.exe"
offset = "0x4477808"
hops = [
    "+0x110",
    "+0x40",
    "+0x20",
    "+0x2b8",
    "+0x20",
    "+0x180",
    "+0x1f4",
]
