[game]
name = "Dispatch"
exe = "dispatch-win64-shipping.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=37264"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "grab-hero-list-enable-only-during-dispatch-screen"
name = "→ Grab Hero List (Enable Only During Dispatch Screen!)"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(HeroStats,Dispatch-Win64-Shipping.exe,488BxxFFxxxxxxxxxx4889xxxxxxxxxx8Bxxxxxxxxxx89xxxxxxxxxx4C)
alloc(newmem,$1000,HeroStats)
alloc(HeroList,8)
registersymbol(HeroList)
label(code return)

newmem:
  mov [HeroList],rcx

code:
  mov rax,[rcx]
  call qword ptr [rax+000006B8]
  jmp return

HeroStats:
  jmp newmem
  nop 4
return:
registersymbol(HeroStats)

[DISABLE]

HeroStats:
  db 48 8B 01 FF 90 B8 06 00 00
unregistersymbol(*)
dealloc(*)"""
