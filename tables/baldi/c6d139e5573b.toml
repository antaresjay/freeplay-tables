[game]
name = "Baldi's Basics Classic Remastered"
exe = "baldi.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=34538"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "invincible"
name = "Invincible"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,PlayerManager:Update)
define(bytes,55 48 8B EC 48 83 EC 30)
//Send@FearkessRevolution|Sintrix
[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,PlayerManager:Update)

label(code)
label(return)

newmem:
mov [rcx+75],1

code:
  push rbp
  mov rbp,rsp
  sub rsp,30
  jmp return

address:
  jmp newmem
  nop 3
return:

[DISABLE]

address:
  db bytes

dealloc(*)"""

[[cheat]]
id = "stealth-mode"
name = "Stealth Mode"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,PlayerManager:Update+1c)
define(bytes,48 8B C8 48 BA F0 F3 13 15 C0 02 00 00)

[ENABLE]
//Send@FearkessRevolution|Sintrix
assert(address,bytes)
alloc(newmem,$1000,PlayerManager:Update+1c)

label(code)
label(return)

newmem:
mov [rcx+76],1
code:
  mov rcx,rax
  mov rdx,000002C01513F3F0
  jmp return

address:
  jmp newmem
  nop 8
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "infinite-stamina"
name = "Infinite Stamina"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,PlayerMovement:StaminaUpdate)
define(bytes,55 48 8B EC 48 81 EC 80 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,PlayerMovement:StaminaUpdate)
//Send@Sintrix|FearlessRevolution
label(code)
label(return)

newmem:
mov [rcx+A0],(float)100
code:
  push rbp
  mov rbp,rsp
  sub rsp,00000080
  jmp return

address:
  jmp newmem
  nop 6
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "disable-baldi"
name = "Disable Baldi"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,Baldi:Update)
define(bytes,55 48 8B EC 48 83 EC 40)

[ENABLE]
//Send@Sintrix|FearlessRevolution
assert(address,bytes)
alloc(newmem,$1000,Baldi:Update)

label(code)
label(return)

newmem:
mov [rcx+153],1
mov [rcx+13c],(float)0

code:
  push rbp
  mov rbp,rsp
  sub rsp,40
  jmp return

address:
  jmp newmem
  nop 3
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""
