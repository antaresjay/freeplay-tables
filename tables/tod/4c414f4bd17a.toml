[game]
name = "Total Overdose"
exe = "tod.exe"
notes = "Converted from a Cheat Engine table by 1lvl1. Source: https://fearlessrevolution.com/viewtopic.php?t=23141"
verified = []
author = "1lvl1"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "hp"
name = "hp"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(hp,TOD.exe,20 F3 0F 11 46 20 A1) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [esi+20],(float)0
  jmp return

hp+01:
  jmp newmem
return:
registersymbol(hp)

[DISABLE]

hp+01:
  db F3 0F 11 46 20

unregistersymbol(hp)
dealloc(newmem)"""

[[cheat]]
id = "adrenalin"
name = "adrenalin"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(adrenalin,TOD.exe,F3 0F 5C 41 3C) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [ecx+3C],(float)0
  jmp return

adrenalin:
  jmp newmem
return:
registersymbol(adrenalin)

[DISABLE]

adrenalin:
  db F3 0F 5C 41 3C

unregistersymbol(adrenalin)
dealloc(newmem)"""

[[cheat]]
id = "rewind"
name = "rewind"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(rewind,TOD.exe,8B 04 81 83 F8 01) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [ecx+eax*4],5
  mov eax,[ecx+eax*4]
  cmp eax,01
  jmp return

rewind:
  jmp newmem
  nop
return:
registersymbol(rewind)

[DISABLE]

rewind:
  db 8B 04 81 83 F8 01

unregistersymbol(rewind)
dealloc(newmem)"""

[[cheat]]
id = "move"
name = "move"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(move,TOD.exe,8B 04 82 48 89 44 24 30) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [edx+eax*4],5
  mov eax,[edx+eax*4]
  mov [esp+30],eax
  jmp return

move:
  jmp newmem
  nop 3
return:
registersymbol(move)

[DISABLE]

move:
  db 8B 04 82 48 89 44 24 30

unregistersymbol(move)
dealloc(newmem)"""

[[cheat]]
id = "ammo"
name = "ammo"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(clip,TOD.exe,8B 48 64 3B 48 68) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [eax+64],#999
  mov ecx,[eax+64]
  cmp ecx,[eax+68]
  jmp return

clip:
  jmp newmem
  nop
return:
registersymbol(clip)

[DISABLE]

clip:
  db 8B 48 64 3B 48 68

unregistersymbol(clip)
dealloc(newmem)"""

[[cheat]]
id = "clip"
name = "clip"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(ammo,TOD.exe,8B 04 91 85 C0 89) // should be unique
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov [ecx+edx*4],#100
  mov eax,[ecx+edx*4]
  test eax,eax
  jmp return

ammo:
  jmp newmem
return:
registersymbol(ammo)

[DISABLE]

ammo:
  db 8B 04 91 85 C0

unregistersymbol(ammo)
dealloc(newmem)"""
