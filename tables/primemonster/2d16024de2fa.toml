[game]
name = "Prime Monster"
exe = "primemonster.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=39336"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "enable-game-session-cheats"
name = "➜ Enable Game Session Cheats"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,GameSession.Update)
define(bytes,48 89 5C 24 10)

[ENABLE]
//Send @ https://sintrix.net
assert(address,bytes)
alloc(newmem,$1000,GameSession.Update)
registersymbol(gsHook)
label(code return gsHook)
// Send @ Sintrix.net
newmem:
mov [gsHook],rcx

code:
  mov [rsp+10],rbx
  jmp return
// Send @ Sintrix.net
gsHook:
dq 0

address:
  jmp newmem
return:
//Send @ https://sintrix.net
[DISABLE]

address:
  db bytes

  unregistersymbol(*)
  dealloc(*)"""

[[cheat]]
id = "unlimited-card-uses"
name = "Unlimited Card Uses"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,Tactic.Op_AdjustTurnUsesLeft)
define(bytes,44 8B 41 2C 42 8D 04 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,Tactic.Op_AdjustTurnUsesLeft)

label(code)
label(return)

newmem:
mov [rcx+2C],#99
code:
  mov r8d,[rcx+2C]
  lea eax,[rdx+r8]
  jmp return

address:
  jmp newmem
  nop 3
return:

[DISABLE]

address:
  db bytes


dealloc(newmem)"""
