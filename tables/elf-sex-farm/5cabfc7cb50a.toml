[game]
name = "Elf Sex Farm"
exe = "elf sex farm.exe"
notes = "Converted from a Cheat Engine table by Firstlast2s100. Source: https://fearlessrevolution.com/viewtopic.php?t=38308"
verified = []
author = "Firstlast2s100"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "playeractor"
name = "PlayerActor"
category = "player"
description = ""
hint = ""
type = "script"
source = """
{ Game   : Elf Sex Farm.exe
  Version: 
  Date   : 2026-02-21
  Author : DANGER DASH

  This script does blah blah blah
}

define(address,PlayerActor:Update)
define(bytes,55 48 8B EC 48 81 EC D0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,PlayerActor:Update)

label(code)
label(return)

newmem:
mov [playeractor_ptr],rcx

code:
  push rbp
  mov rbp,rsp
  sub rsp,000000D0
  jmp return

playeractor_ptr:
dq 0

address:
  jmp newmem
  nop 6
return:
registersymbol(playeractor_ptr)

[DISABLE]

address:
  db bytes
  // push rbp
  // mov rbp,rsp
  // sub rsp,000000D0

dealloc(newmem)
unregistersymbol(playeractor_ptr)"""

[[cheat]]
id = "inventorydata"
name = "InventoryData"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
{ Game   : Elf Sex Farm.exe
  Version: 
  Date   : 2026-02-21
  Author : DANGER DASH

  This script does blah blah blah
}

define(address,InventoryData:Add)
define(bytes,55 48 8B EC 48 83 EC 50)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,InventoryData:Add)

label(code)
label(return)

newmem:
mov [inventorydata_ptr],rcx

code:
  push rbp
  mov rbp,rsp
  sub rsp,50
  jmp return

inventorydata_ptr:
dq 0

address:
  jmp newmem
  nop 3
return:
registersymbol(inventorydata_ptr)

[DISABLE]

address:
  db bytes
  // push rbp
  // mov rbp,rsp
  // sub rsp,50

dealloc(newmem)
unregistersymbol(inventorydata_ptr)"""

[[cheat]]
id = "updatedaytimertag"
name = "UpdateDayTimerTag"
category = "game"
description = ""
hint = ""
type = "script"
source = """
{ Game   : Elf Sex Farm.exe
  Version: 
  Date   : 2026-02-21
  Author : DANGER DASH

  This script does blah blah blah
}

define(address,WorldManager:UpdateDayTimerTag+12)
define(bytes,F3 0F 10 00 F3 0F 5A C0)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,WorldManager:UpdateDayTimerTag+12)

label(code)
label(return)

newmem:
mov [UDTT_ptr],rax

code:
  movss xmm0,[rax]
  cvtss2sd xmm0,xmm0
  jmp return

UDTT_ptr:
dq 0

address:
  jmp newmem
  nop 3
return:
registersymbol(UDTT_ptr)

[DISABLE]

address:
  db bytes
  // movss xmm0,[rax]
  // cvtss2sd xmm0,xmm0

dealloc(newmem)
unregistersymbol(UDTT_ptr)"""

[[cheat]]
id = "accountingmanager"
name = "AccountingManager"
category = "player"
description = ""
hint = ""
type = "script"
source = """
{ Game   : Elf Sex Farm.exe
  Version: 
  Date   : 2026-02-21
  Author : DANGER DASH

  This script does blah blah blah
}

define(address,ACTStateUI:Update+267)
define(bytes,48 63 09 3B C1)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,ACTStateUI:Update+267)

label(code)
label(return)

newmem:
mov [actstateUI_ptr],rcx

code:
  movsxd  rcx,dword ptr [rcx]
  cmp eax,ecx
  jmp return

actstateUI_ptr:
dq 0

address:
  jmp newmem
return:
registersymbol(actstateUI_ptr)

[DISABLE]

address:
  db bytes
  // movsxd  rcx,dword ptr [rcx]
  // cmp eax,ecx

dealloc(newmem)
unregistersymbol(actstateUI_ptr)"""
