[game]
name = "Roller Coaster Tycoon: Deluxe Edition"
exe = "rct.exe"
notes = "Converted from a Cheat Engine table by frogger9801. Source: https://fearlessrevolution.com/viewtopic.php?t=7517"
verified = []
author = "frogger9801"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "infinite-cash"
name = "Infinite Cash"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FE)
define(bytes,A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov eax, 989680
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "2x"
name = "2x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ebx, 0
  push eax
  mov eax, 2
  mul ebx
  mov ebx, eax
  pop eax
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "3x"
name = "3x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ebx, 0
  push eax
  mov eax, 3
  mul ebx
  mov ebx, eax
  pop eax
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "5x"
name = "5x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ebx, 0
  push eax
  mov eax, 5
  mul ebx
  mov ebx, eax
  pop eax
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "10x"
name = "10x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ebx, 0
  push eax
  mov eax, A
  mul ebx
  mov ebx, eax
  pop eax
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "2x-2"
name = "2x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)
label(jumper)

newmem:

code:
  cmp ebx, 0
  jle jumper
  push eax
  mov eax, 2
  mul ebx
  mov ebx, eax
  pop eax
jumper:
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "3x-2"
name = "3x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)
label(jumper)

newmem:

code:
  cmp ebx, 0
  jle jumper
  push eax
  mov eax, 3
  mul ebx
  mov ebx, eax
  pop eax
jumper:
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "5x-2"
name = "5x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)
label(jumper)

newmem:

code:
  cmp ebx, 0
  jle jumper
  push eax
  mov eax, 5
  mul ebx
  mov ebx, eax
  pop eax
jumper:
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "10x-2"
name = "10x"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+386FA)
define(bytes,2B C3 70 51 A3 90 C5 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)
label(jumper)

newmem:

code:
  cmp ebx, 0
  jle jumper
  push eax
  mov eax, A
  mul ebx
  mov ebx, eax
  pop eax
jumper:
  sub eax,ebx
  jo rct.exe+3874F
  mov [rct.exe+69C590],eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // sub eax,ebx
  // jo rct.exe+3874F
  // mov [rct.exe+69C590],eax

dealloc(newmem)"""

[[cheat]]
id = "100m-company-value"
name = "$100M Company Value"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F533)
define(bytes,A3 00 D9 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov [rct.exe+69D900],3B9ACA00
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [rct.exe+69D900],eax

dealloc(newmem)"""

[[cheat]]
id = "100m-park-value"
name = "$100M Park Value"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F529)
define(bytes,A3 F0 D6 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov [rct.exe+69D6F0],3B9ACA00
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [rct.exe+69D6F0],eax

dealloc(newmem)"""

[[cheat]]
id = "999-park-rating"
name = "999 Park Rating"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F51E)
define(bytes,66 A3 64 CE A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ax, 3E7
  mov [rct.exe+69CE64],ax
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [rct.exe+69CE64],ax

dealloc(newmem)"""

[[cheat]]
id = "always-win-time-based-objectives"
name = "Always Win Time Based Objectives"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+120B7)
define(bytes,C3 C7 05 F4 D8 A9 00 01 00 00 80 E9 1B 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov [rct.exe+69D8F4],80000001
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
  nop
  nop
  nop
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // ret 
  // mov [rct.exe+69D8F4],80000001

dealloc(newmem)"""

[[cheat]]
id = "allow-construction-while-paused"
name = "Allow Construction While Paused"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+162921)
define(address2,"rct.exe"+3F047)
define(address3,"rct.exe"+3F459)
define(address4,"rct.exe"+CC87)
define(address5,"rct.exe"+178102)
define(address6,"rct.exe"+175235)
define(address7,"rct.exe"+1754C4)
define(address8,"rct.exe"+162F59)
define(address9,"rct.exe"+4A8DD)
define(address10,"rct.exe"+4ABC7)
define(address11,"rct.exe"+177AF4)
define(address12,"rct.exe"+177EA3)
define(address13,"rct.exe"+EA61)
define(address14,"rct.exe"+EC96)
define(address15,"rct.exe"+479F1)
define(address16,"rct.exe"+16B6F3)
define(address17,"rct.exe"+D6D0)
define(bytes,80 3D D7 5F 8B 00 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
assert(address3,bytes)
assert(address4,bytes)
assert(address5,bytes)
assert(address6,bytes)
assert(address7,bytes)
assert(address8,bytes)
assert(address9,bytes)
assert(address10,bytes)
assert(address11,bytes)
assert(address12,bytes)
assert(address13,bytes)
assert(address14,bytes)
assert(address15,bytes)
assert(address16,bytes)
assert(address17,bytes)
alloc(newmem,$256)

label(code)
label(return)

newmem:

code:
  cmp ax,ax
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop
address3:
  call code
  nop
  nop
address4:
  call code
  nop
  nop
address5:
  call code
  nop
  nop
address6:
  call code
  nop
  nop
address7:
  call code
  nop
  nop
address8:
  call code
  nop
  nop
address9:
  call code
  nop
  nop
address10:
  call code
  nop
  nop
address11:
  call code
  nop
  nop
address12:
  call code
  nop
  nop
address13:
  call code
  nop
  nop
address14:
  call code
  nop
  nop
address15:
  call code
  nop
  nop
address16:
  call code
  nop
  nop
address17:
  call code
  nop
  nop
return:

[DISABLE]

address1:
  db bytes
address2:
  db bytes
address3:
  db bytes
address4:
  db bytes
address5:
  db bytes
address6:
  db bytes
address7:
  db bytes
address8:
  db bytes
address9:
  db bytes
address10:
  db bytes
address11:
  db bytes
address12:
  db bytes
address13:
  db bytes
address14:
  db bytes
address15:
  db bytes
address16:
  db bytes
address17:
  db bytes
  // cmp byte ptr [rct.exe+4B5FD7],00

dealloc(newmem)"""

[[cheat]]
id = "allow-ride-construction-through-objects-land"
name = "Allow Ride Construction Through Objects/Land"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+162A5B)
define(bytes1,0F 82 32 04 00 00)
define(address2,"rct.exe"+162A86)
define(bytes2,0F 82 07 04 00 00)
define(address3,"rct.exe"+162AF0)
define(bytes3,0F 85 73 03 00 00)

[ENABLE]

assert(address1,bytes1)
alloc(newmem1,$16)
assert(address2,bytes2)
alloc(newmem2,$16)
assert(address3,bytes3)
alloc(newmem3,$16)

label(code1)
label(return1)
label(code2)
label(return2)
label(code3)
label(return3)

newmem1:

code1:
  jmp return1

address1:
  jmp code1
  nop
return1:

newmem2:

code2:
  jmp return2

address2:
  jmp code2
  nop
return2:

newmem3:

code3:
  jmp return3

address3:
  jmp code3
  nop
return3:

[DISABLE]

address1:
  db bytes1
  // jb rct.exe+162E93
address2:
  db bytes2
  // jb rct.exe+162E93
address3:
  db bytes3
  // jne rct.exe+162E69

dealloc(newmem1)
dealloc(newmem2)
dealloc(newmem3)"""

[[cheat]]
id = "allow-land-and-water-adjustments-outside-park"
name = "Allow Land and Water Adjustments Outside Park"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+DB06)
define(bytes,F6 46 07 20 74 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  push ax
  mov ax, 0
  cmp ax, -1
  pop ax
  je rct.exe+DB10
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // test byte ptr [esi+07],20
  // je rct.exe+DB10

dealloc(newmem)"""

[[cheat]]
id = "allow-landscaping-and-building-outside-park"
name = "Allow Landscaping and Building Outside Park"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+DB4F)
define(bytes,F6 46 07 20 75 26)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  cmp ax, FF
  jne rct.exe+DB7B
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // test byte ptr [esi+07],20
  // jne rct.exe+DB7B

dealloc(newmem)"""

[[cheat]]
id = "buy-construction-rights-anywhere"
name = "Buy Construction Rights Anywhere"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+D7C8)
define(bytes,F6 C2 40 0F 84 B7 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  push ax
  mov ax, 0
  cmp ax, -1
  pop ax
  je rct.exe+D888
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // test dl,40
  // je rct.exe+D888

dealloc(newmem)"""

[[cheat]]
id = "buy-land-anywhere"
name = "Buy Land Anywhere"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+D736)
define(bytes,F6 C2 80 0F 84 37 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  push ax
  mov ax, 0
  cmp ax, -1
  pop ax
  je rct.exe+D876
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // test dl,-80
  // je rct.exe+D876

dealloc(newmem)"""

[[cheat]]
id = "new-handymen-have-mow-grass-disabled"
name = "New Handymen Have 'Mow Grass' Disabled"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F75E)
define(bytes,C6 86 C6 00 00 00 0F)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+000000C6],07
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+000000C6],0F

dealloc(newmem)"""

[[cheat]]
id = "fast-research"
name = "Fast Research"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2588C)
define(bytes,0F 85 A7 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$8)

label(code)
label(return)

newmem:

code:
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // jne rct.exe+25939

dealloc(newmem)"""

[[cheat]]
id = "set-to-march-1st-year-1"
name = "Set to March 1st, Year 1"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5479D)
define(bytes,66 A1 5E 3D 90 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ax,[rct.exe+503D5E]
  push ax
  mov ax, 0
  mov [rct.exe+503D5C],ax
  mov [rct.exe+503D5E],ax
  pop ax
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // mov ax,[rct.exe+503D5E]

dealloc(newmem)"""

[[cheat]]
id = "0x"
name = "0x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],0
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "0-25x"
name = "0.25x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],1
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "0-5x"
name = "0.5x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],2
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "1-5x"
name = "1.5x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],6
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "2x-3"
name = "2x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],8
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "3x-3"
name = "3x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],C
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "5x-3"
name = "5x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],14
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "10x-3"
name = "10x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],28
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "20x"
name = "20x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],50
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "50x"
name = "50x"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],64
  add word ptr [rct.exe+503D5E],64
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "max"
name = "Max"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+547B1)
define(bytes,66 83 05 5E 3D 90 00 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  add word ptr [rct.exe+503D5E],FF
  jmp return

address:
  jmp code
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // add word ptr [rct.exe+503D5E],04

dealloc(newmem)"""

[[cheat]]
id = "sunny"
name = "Sunny"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5483A)
define(bytes,8A 44 19 02 E8 27 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,0
  call rct.exe+5486A
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov al,[ecx+ebx+02]
  // call rct.exe+5486A

dealloc(newmem)"""

[[cheat]]
id = "partially-cloudy"
name = "Partially Cloudy"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5483A)
define(bytes,8A 44 19 02 E8 27 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,1
  call rct.exe+5486A
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov al,[ecx+ebx+02]
  // call rct.exe+5486A

dealloc(newmem)"""

[[cheat]]
id = "cloudy"
name = "Cloudy"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5483A)
define(bytes,8A 44 19 02 E8 27 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,2
  call rct.exe+5486A
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov al,[ecx+ebx+02]
  // call rct.exe+5486A

dealloc(newmem)"""

[[cheat]]
id = "rain"
name = "Rain"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5483A)
define(bytes,8A 44 19 02 E8 27 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,3
  call rct.exe+5486A
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov al,[ecx+ebx+02]
  // call rct.exe+5486A

dealloc(newmem)"""

[[cheat]]
id = "heavy-rain"
name = "Heavy Rain"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5483A)
define(bytes,8A 44 19 02 E8 27 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,4
  call rct.exe+5486A
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov al,[ecx+ebx+02]
  // call rct.exe+5486A

dealloc(newmem)"""

[[cheat]]
id = "thunderstorm"
name = "Thunderstorm"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5483A)
define(bytes,8A 44 19 02 E8 27 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,5
  call rct.exe+5486A
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov al,[ecx+ebx+02]
  // call rct.exe+5486A

dealloc(newmem)"""

[[cheat]]
id = "allow-loading-of-modded-save-files"
name = "Allow Loading of Modded Save Files"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+19B6B)
define(bytes,0F 82 2F 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  jmp rct.exe+19B87
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // jb rct.exe+19CA0

dealloc(newmem)"""

[[cheat]]
id = "new-guests-have-10k"
name = "New Guests Have $10k"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+369B2)
define(bytes,89 86 A0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov eax, 186A0
  mov [esi+000000A0],eax
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [esi+000000A0],eax

dealloc(newmem)"""

[[cheat]]
id = "zero"
name = "Zero"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F566)
define(bytes,66 3B 05 A0 D2 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ax,0
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // cmp ax,[rct.exe+69D2A0]

dealloc(newmem)"""

[[cheat]]
id = "low"
name = "Low"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F566)
define(bytes,66 3B 05 A0 D2 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ax,20
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // cmp ax,[rct.exe+69D2A0]

dealloc(newmem)"""

[[cheat]]
id = "high"
name = "High"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F566)
define(bytes,66 3B 05 A0 D2 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ax,700
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // cmp ax,[rct.exe+69D2A0]

dealloc(newmem)"""

[[cheat]]
id = "insane"
name = "Insane"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+F566)
define(bytes,66 3B 05 A0 D2 A9 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  cmp ax,-1
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // cmp ax,[rct.exe+69D2A0]

dealloc(newmem)"""

[[cheat]]
id = "0"
name = "0"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,00
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "1"
name = "1"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,01
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "2"
name = "2"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov ah,02
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "3"
name = "3"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,03
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "4"
name = "4"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,04
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "5"
name = "5"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,05
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "6"
name = "6"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,06
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "7"
name = "7"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,07
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "8"
name = "8"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,08
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "9"
name = "9"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,09
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "10"
name = "10"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0A
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "11"
name = "11"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0B
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "12"
name = "12"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0C
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "13"
name = "13"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0D
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "14"
name = "14"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0E
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "15"
name = "15"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3685E)
define(bytes,0A C4 88 46 43)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0F
  or al,ah
  mov [esi+43],al
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // or al,ah
  // mov [esi+43],al

dealloc(newmem)"""

[[cheat]]
id = "0-2"
name = "0"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,00
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "1-2"
name = "1"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,10
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "2-2"
name = "2"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,20
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "3-2"
name = "3"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,30
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "4-2"
name = "4"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,40
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "5-2"
name = "5"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,50
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "6-2"
name = "6"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,60
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "7-2"
name = "7"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,70
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "8-2"
name = "8"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,80
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "9-2"
name = "9"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,90
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "10-2"
name = "10"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,A0
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "11-2"
name = "11"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,B0
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "12-2"
name = "12"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,C0
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "13-2"
name = "13"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,D0
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "14-2"
name = "14"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,E0
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "15-2"
name = "15"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36855)
define(bytes,73 04 B4 09 B0 0F C0 E0 04)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov al,F0
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // jae rct.exe+3685B
  // mov ah,09
  // mov al,0F

dealloc(newmem)"""

[[cheat]]
id = "none"
name = "None"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3687B)
define(bytes,88 46 44 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov al,0
  mov [esi+44],al
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+44],al
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "low-2"
name = "Low"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3687B)
define(bytes,88 46 44 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov al,1
  mov [esi+44],al
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+44],al
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "average"
name = "Average"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3687B)
define(bytes,88 46 44 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov al,2
  mov [esi+44],al
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+44],al
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "high-2"
name = "High"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3687B)
define(bytes,88 46 44 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov al,3
  mov [esi+44],al
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+44],al
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "black"
name = "Black"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 0
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "gray"
name = "Gray"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "white"
name = "White"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 2
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-purple"
name = "Dull Purple"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 3
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "purple"
name = "Purple"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 4
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dark-blue"
name = "Dark Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 5
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "blue"
name = "Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 6
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "teal"
name = "Teal"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 7
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "green"
name = "Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 8
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-green"
name = "Dull Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 9
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-light-green"
name = "Dull Light Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, A
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-green"
name = "Light Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, B
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "forest-green"
name = "Forest Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, C
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "green-tan"
name = "Green/Tan"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, D
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "yellow"
name = "Yellow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, E
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "brown"
name = "Brown"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, F
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-orange"
name = "Light Orange"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 10
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "orange"
name = "Orange"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 11
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-yellow"
name = "Dull Yellow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 12
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "tan"
name = "Tan"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 13
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "tan-gray"
name = "Tan/Gray"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 14
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "peach"
name = "Peach"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 15
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "merlot"
name = "Merlot"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 16
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dark-red"
name = "Dark Red"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 17
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "red"
name = "Red"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 18
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "hot-pink"
name = "Hot Pink"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 19
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-pink"
name = "Light Pink"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1A
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "pink"
name = "Pink"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1B
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-dark-blue"
name = "Dull Dark Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1C
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "cyan"
name = "Cyan"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1D
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-yellow"
name = "Light Yellow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1E
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "baby-blue"
name = "Baby Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 1F
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "null-1"
name = "NULL 1"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 20
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "null-2"
name = "NULL 2"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A53)
define(bytes,88 66 30 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah, 21
  mov [esi+30],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+30],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "black-2"
name = "Black"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,00
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "gray-2"
name = "Gray"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,01
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "white-2"
name = "White"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,02
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-purple-2"
name = "Dull Purple"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,03
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "purple-2"
name = "Purple"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,04
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dark-blue-2"
name = "Dark Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,05
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "blue-2"
name = "Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,06
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "teal-2"
name = "Teal"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,07
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "green-2"
name = "Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,08
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-green-2"
name = "Dull Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,09
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-light-green-2"
name = "Dull Light Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0A
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-green-2"
name = "Light Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0B
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "forest-green-2"
name = "Forest Green"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0C
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "green-tan-2"
name = "Green/Tan"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0D
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "yellow-2"
name = "Yellow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0E
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "brown-2"
name = "Brown"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,0F
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-orange-2"
name = "Light Orange"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,10
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "orange-2"
name = "Orange"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,11
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-yellow-2"
name = "Dull Yellow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,12
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "tan-2"
name = "Tan"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,13
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "tan-gray-2"
name = "Tan/Gray"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,14
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "peach-2"
name = "Peach"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,15
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "merlot-2"
name = "Merlot"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,16
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dark-red-2"
name = "Dark Red"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,17
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "red-2"
name = "Red"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,18
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "hot-pink-2"
name = "Hot Pink"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,19
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-pink-2"
name = "Light Pink"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,1A
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "pink-2"
name = "Pink"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,1B
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "dull-dark-blue-2"
name = "Dull Dark Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,1C
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "cyan-2"
name = "Cyan"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,1D
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "light-yellow-2"
name = "Light Yellow"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,1E
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "baby-blue-2"
name = "Baby Blue"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+36A5F)
define(bytes,88 66 31 8B C5)

[ENABLE]

assert(address,bytes)
alloc(newmem,$64)

label(code)
label(return)

newmem:

code:
  mov ah,1F
  mov [esi+31],ah
  mov eax,ebp
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // mov [esi+31],ah
  // mov eax,ebp

dealloc(newmem)"""

[[cheat]]
id = "have-all-rides"
name = "Have All Rides"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+D692)
define(bytes1,0F 85 17 FE FF FF)
define(address2,"rct.exe"+25720)
define(bytes2,0F A3 0C 95 B8 C5 A9 00)

[ENABLE]

assert(address1,bytes1)
assert(address2,bytes2)
alloc(newmem,$100)

label(code1)
label(code2)

newmem:

code1:
  push ax
  mov ax, FFAD
  mov [rct.exe+69D97E], ax
  mov ax, FFFF
  mov [rct.exe+69C5B8], ax
  mov ax, FFFF
  mov [rct.exe+69C5BA], ax
  mov ax, FFFF
  mov [rct.exe+69C5BC], ax
  mov ax, FFFF
  mov [rct.exe+69C5BE], ax
  mov ax, FFEE
  mov [rct.exe+69C5C0], ax
  mov ax, 001F
  mov [rct.exe+69C5C2], ax
  pop ax
  ret
code2:
  ret
address1:
  call code1
  nop
address2:
  jmp code2
  nop
  nop
  nop

[DISABLE]

address1:
  db bytes1
  // jne rct.exe+D4AF
address2:
  db bytes2
  // bt [edx*4+rct.exe+69C5B8],ecx

dealloc(newmem)"""

[[cheat]]
id = "rides-never-break-down"
name = "Rides Never Break Down"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+49CE5)
define(bytes,0F 85 21 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  jmp rct.exe+49E0C
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // jne rct.exe+49E0C

dealloc(newmem)"""

[[cheat]]
id = "unlimited-station-length"
name = "Unlimited Station Length"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1626EF)
define(bytes,0F 87 87 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$8)

label(code)
label(return)

newmem:

code:
  jmp return

address:
  jmp code
  nop
return:

[DISABLE]

address:
  db bytes
  // ja rct.exe+16287C

dealloc(newmem)"""

[[cheat]]
id = "ride-height-not-restricted-by-local-authority"
name = "Ride Height not Restricted by Local Authority"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+162A56)
define(bytes,E8 F2 8C EC FF)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  clc
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // call rct.exe+2B74D

dealloc(newmem)"""

[[cheat]]
id = "no-ride-support-limits"
name = "No Ride Support Limits"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+162BCC)
define(bytes,74 08 83 C1 08)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  jmp rct.exe+162BF8
  add ecx,08
  jmp return

address:
  jmp code
return:

[DISABLE]

address:
  db bytes
  // je rct.exe+162BD6
  // add ecx,08

dealloc(newmem)"""

[[cheat]]
id = "no-launch-speed-limits"
name = "No Launch Speed Limits"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+45298)
define(bytes1,73 02 02 FB EB 33)
define(address2,"rct.exe"+452C6)
define(bytes2,3A 3C CD 24 8E 78 00)

[ENABLE]

assert(address1,bytes1)
alloc(newmem1,$32)
assert(address2,bytes2)
alloc(newmem2,$32)

label(code1)
label(return1)
label(code2)
label(return2)

newmem1:

code1:
  add bh,bl
  jmp rct.exe+452D1
  jmp return1

address1:
  jmp code1
  nop
return1:

newmem2:

code2:
  push ax
  mov ax,1
  cmp ax,0
  pop ax
  jmp return2

address2:
  jmp code2
  nop
return2:

[DISABLE]

address1:
  db bytes1
  // jae rct.exe+4529C
  // add bh,bl
  // jmp rct.exe+452D1
address2:
  db bytes2
  // cmp bh,[ecx*8+rct.exe+388E24]

dealloc(newmem1)
dealloc(newmem2)"""

[[cheat]]
id = "2mph"
name = "2mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00010000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "5mph"
name = "5mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00025F04
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "7mph"
name = "7mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00034F04
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "10mph"
name = "10mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00048000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "15mph"
name = "15mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00070000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "20mph"
name = "20mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00090000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "30mph"
name = "30mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],000D9000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "40mph"
name = "40mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],00120000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "60mph"
name = "60mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],001B0000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "100mph"
name = "100mph"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address1,"rct.exe"+16F813)
define(address2,"rct.exe"+16FBAE)
define(bytes,81 7E 28 04 5F 02 00)

[ENABLE]

assert(address1,bytes)
assert(address2,bytes)
alloc(newmem,$16)

label(code)

newmem:

code:
  cmp [esi+28],002C8000
  ret

address1:
  call code
  nop
  nop
address2:
  call code
  nop
  nop

[DISABLE]

address1:
  db bytes
  // cmp [esi+28],00025F04
address2:
  db bytes
  // cmp [esi+28],00025F04

dealloc(newmem)"""

[[cheat]]
id = "20-excitement"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+22AA)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+22B1)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+22B8)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-2"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2472)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-2"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2479)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-2"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2480)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-3"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6606)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-3"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+660D)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-3"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6614)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-4"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3CBB)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-4"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3CC2)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-4"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3CC9)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-5"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+71B1)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-5"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+71B8)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-5"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+71BF)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-6"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6FB3)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-6"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6FBA)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-6"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6FC1)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-7"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F05)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-7"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F0C)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-7"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F13)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-8"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4875)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-8"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+487C)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-8"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4883)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-9"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4001)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-9"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4008)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-9"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+400F)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-10"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+47E4)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-10"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+47EB)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-10"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+47F2)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-11"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6C06)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-11"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6C0D)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-11"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6C14)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-12"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3328)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-12"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+332F)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-12"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3336)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-13"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2EFF)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-13"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2F06)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-13"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2F0D)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-14"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+61E8)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-14"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+61EF)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-14"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+61F6)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-15"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+407F)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-15"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4086)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-15"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+408D)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-16"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+463B)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-16"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4642)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-16"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4649)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-17"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4A52)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-17"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4A59)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-17"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4A60)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-18"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F61)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-18"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F68)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-18"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4F6F)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-19"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+514F)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-19"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5156)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-19"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+515D)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-20"
name = "20 Excitement"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6F5F)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-20"
name = "3 Intensity"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6F66)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-20"
name = "1 Nausea"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6F6D)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-21"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5907)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+590E)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-21"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5915)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-22"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5E15)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-2"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5E1C)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-22"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5E23)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-23"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+688A)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-3"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6891)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-23"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6898)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-24"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+16EB)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-4"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+16F2)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-24"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+16F9)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-25"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5690)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-5"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5697)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-25"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+569E)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-26"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2A76)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-6"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2A7D)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-26"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2A84)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-27"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5B9F)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-7"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5BA6)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-27"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5BAD)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-28"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7ABC)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-8"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7AC3)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-28"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7ACA)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-29"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+210B)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-9"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2112)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-29"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2119)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-30"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3AC9)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-10"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3AD0)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-30"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3AD7)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-31"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+35DD)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-11"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+35E4)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-31"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+35EB)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-32"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+195B)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-12"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1962)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-32"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1969)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-33"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3F7B)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-13"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3F82)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-33"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3F89)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-34"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5409)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-14"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5410)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-34"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5417)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-35"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1BEA)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-15"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1BF1)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-35"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1BF8)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-36"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1E78)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-16"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1E7F)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-36"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+1E86)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-37"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7442)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-17"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7449)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-37"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7450)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-38"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2706)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-18"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+270D)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-38"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2714)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-39"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2CFD)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-19"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2D04)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-39"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2D0B)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-40"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3259)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-20"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3260)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-40"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3267)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-41"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6AD7)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-21"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6ADE)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-41"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6AE5)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-42"
name = "20 Excitement"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6467)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-22"
name = "5 Intensity"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+646E)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-42"
name = "1 Nausea"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6475)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-43"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4BC3)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-23"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4BCA)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-43"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4BD1)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-44"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4E2A)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-24"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4E31)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-44"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4E38)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-45"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7824)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-25"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+782B)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-45"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7832)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-46"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4EB1)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-26"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4EB8)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-46"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4EBF)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-47"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2FF6)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-27"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+2FFD)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-47"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3004)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-48"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+46BF)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-28"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+46C6)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-48"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+46CD)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-49"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+41ED)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-29"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+41F4)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-49"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+41FB)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-50"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4743)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-30"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+474A)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-50"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4751)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-51"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+48D8)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-31"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+48DF)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-51"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+48E6)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-52"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+494A)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-32"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4951)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]


assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-52"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4958)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-53"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+49E0)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-33"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+49E7)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-53"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+49EE)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-54"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6ED1)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-34"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6ED8)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-54"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+6EDF)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-55"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7D8E)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-35"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7D95)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-55"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7D9C)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-56"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3842)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-36"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3849)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-56"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+3850)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-57"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7B39)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-21"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7B40)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-57"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7B47)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-58"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+43CC)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-22"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+43D3)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-58"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+43DA)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-59"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+45AD)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-23"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+45B4)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-59"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+45BB)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-60"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5FE7)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-24"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5FEE)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-60"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+5FF5)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-61"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+27D5)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-25"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+27DC)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-61"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+27E3)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-62"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7D01)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "3-intensity-26"
name = "3 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7D08)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 12C
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-62"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+7D0F)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "20-excitement-63"
name = "20 Excitement"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+76B3)
define(bytes,66 89 9F F0 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bx, 7D0
  mov [edi+000000F0],bx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F0],bx

dealloc(newmem)"""

[[cheat]]
id = "5-intensity-37"
name = "5 Intensity"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+76BA)
define(bytes,66 89 8F F2 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov cx, 1F4
  mov [edi+000000F2],cx
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F2],cx

dealloc(newmem)"""

[[cheat]]
id = "1-nausea-63"
name = "1 Nausea"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+76C1)
define(bytes,66 89 AF F4 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov bp, 64
  mov [edi+000000F4],bp
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov [edi+000000F4],bp

dealloc(newmem)"""

[[cheat]]
id = "every-10-mins"
name = "Every 10 Mins"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],00
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "every-20-mins"
name = "Every 20 Mins"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],01
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "every-30-mins"
name = "Every 30 Mins"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],02
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "every-45-mins"
name = "Every 45 Mins"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],03
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "every-hour"
name = "Every Hour"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],04
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "every-2-hours"
name = "Every 2 Hours"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],05
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "never"
name = "Never"
category = "game"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+4764E)
define(bytes,C6 86 4A 01 00 00 02)

[ENABLE]

assert(address,bytes)
alloc(newmem,$16)

label(code)
label(return)

newmem:

code:
  mov byte ptr [esi+0000014A],06
  jmp return

address:
  jmp code
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // mov byte ptr [esi+0000014A],02

dealloc(newmem)"""

[[cheat]]
id = "kill-switch"
name = "Kill Switch"
category = "combat"
description = ""
hint = ""
type = "script"
source = """
define(address,"rct.exe"+16EAF7)
define(bytes,0F B6 7E 50 FF 14 BD 84 ED 56 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$32)

label(code)
label(return)

newmem:

code:
  movzx edi,byte ptr [esi+50]
  cmp edi, 0D
  je return
  cmp edi, 09
  jne rct.exe+16FC57
  push eax
  mov eax, 4
  mul edi
  call dword ptr [eax+rct.exe+16ED84]
  pop eax
  jmp return

address:
  jmp code
  nop
  nop
  nop
  nop
  nop
  nop
return:

[DISABLE]

address:
  db bytes
  // movzx edi,byte ptr [esi+50]
  // call dword ptr [edi*4+rct.exe+16ED84]

dealloc(newmem)"""
