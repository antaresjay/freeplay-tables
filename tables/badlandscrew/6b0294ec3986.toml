[game]
name = "Badlands Crew"
exe = "badlandscrew.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=34824"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "vehicle-invulnerability"
name = "Vehicle Invulnerability"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]
PlayerWheelDamageable.DoDamage:
db C3
PlayerDamageableEngine.DoDamage:
db C3


[DISABLE]
PlayerWheelDamageable.DoDamage:
db 48 8B C4
PlayerDamageableEngine.DoDamage:
db 48 8B C4"""

[[cheat]]
id = "crew-invulnerability"
name = "Crew Invulnerability"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(CrewInvulnerable,GameAssembly.dll,488Bxxxx4885xx0F84xxxxxxxx83xxxxxxxxxxxx488Bxxxxxxxxxx410F)
alloc(newmem,$1000,CrewInvulnerable)
//Send@Sintrix|FearlessRevolution
label(code)
label(return)

newmem:
push r15
mov r15,[rcx+18]
mov [r15+81],1
pop r15

code:
  mov rcx,[rcx+18]
  test rcx,rcx
  jmp return

CrewInvulnerable:
  jmp newmem
  nop 2
return:
registersymbol(CrewInvulnerable)

[DISABLE]

CrewInvulnerable:
  db 48 8B 49 18 48 85 C9
//CrewMemberSelectable.Tick
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "infinite-crew-lives"
name = "Infinite Crew Lives"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(InfCrewLives,GameAssembly.dll,39 9A 98 00 00 00)
alloc(newmem,$1000,InfCrewLives)
//Send@Sintrix|FearlessRevolution
label(code)
label(return)

newmem:

code:
  mov [rdx+00000098],#99
  jmp return

InfCrewLives:
  jmp newmem
  nop
return:
registersymbol(InfCrewLives)

[DISABLE]

InfCrewLives:
  db 39 9A 98 00 00 00
//CrewHealthBar.Tick
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "no-weight"
name = "No Weight"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(VehicleResourceWeight,GameAssembly.dll,66 0F 6E 70 3C)
alloc(newmem,$1000,VehicleResourceWeight)

label(code)
label(return)

newmem:

code:
  mov [rax+3C],#0
  movd xmm6,[rax+3C]
  jmp return

VehicleResourceWeight:
  jmp newmem
return:
registersymbol(VehicleResourceWeight)

[DISABLE]

VehicleResourceWeight:
  db 66 0F 6E 70 3C
//InGameVehicleStatusUI.Tick
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "increased-max-weight"
name = "Increased Max Weight"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(VehicleResourceMaxWeight,GameAssembly.dll,8B 48 38 85 C9)
alloc(newmem,$1000,VehicleResourceMaxWeight)
label(code return)
//Send@FearlessRevolution|Sintrix

newmem:

code:
  mov [rax+38],#9999
  mov ecx,[rax+38]
  test ecx,ecx
  jmp return

VehicleResourceMaxWeight:
  jmp newmem
return:
registersymbol(VehicleResourceMaxWeight)

[DISABLE]
//InGameVehicleStatusUI.Tick
VehicleResourceMaxWeight:
  db 8B 48 38 85 C9
//Send@FearlessRevolution|Sintrix
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "no-radiator-overheat"
name = "No Radiator Overheat"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(RadiatorCoolRate,GameAssembly.dll,F3 0F 10 4B 30 F3 0F 58)
alloc(newmem,$1000,RadiatorCoolRate)
//Send@FearlessRevolution|Sintrix
label(code return)

newmem:
mov [rbx+78],(float)0 //RevsTempWeight
mov [rbx+7C],(float)0 //BoostTempIncrease
mov [rbx+88],(float)0 //DayTime
mov [rbx+8C],(float)0 //NightTime
mov [rbx+90],(float)0 //CurrentAmbient
mov [rbx+94],(float)0 //CurrentCounter
mov [rbx+98],(float)0 //RevsTemp
mov [rbx+9C],(float)0 //SpeedTemp
code:
  mov [rbx+30],(float)100000000 //CoolingRate
  movss xmm1,[rbx+30]
  jmp return

RadiatorCoolRate:
  jmp newmem
return:
registersymbol(RadiatorCoolRate)

[DISABLE]

RadiatorCoolRate:
  db F3 0F 10 4B 30
//Radiator.Tick
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "infinite-fuel"
name = "Infinite Fuel"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(VehicleFuel,GameAssembly.dll,F3 0F 58 70 70 F3 0F 11 B4 24 B0 00 00 00 F3 0F 58 78 38)
alloc(newmem,$1000,VehicleFuel)
//Send@FearlessRevolution|Sintrix
label(code return)

newmem:

code:
  mov [rax+74],0
  mov [rax+70],(float)9999
  addss xmm6,[rax+70]
  jmp return

VehicleFuel:
  jmp newmem
return:
registersymbol(VehicleFuel)

[DISABLE]

VehicleFuel:
  db F3 0F 58 70 70
//VehicleResourceStorage.GetFuelNormalised
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "infinite-boost"
name = "Infinite Boost"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(InfBoostFlag,GameAssembly.dll,80 B8 A0 00 00 00 00 75 0A)
alloc(newmem,$1000,InfBoostFlag)
//Send@FearlessRevolution|Sintrix
label(code return)

newmem:

code:
  mov byte ptr [rax+000000A0],01
  jmp return

InfBoostFlag:
  jmp newmem
  nop 2
return:
registersymbol(InfBoostFlag)

[DISABLE]
//Send@FearlessRevolution|Sintrix
InfBoostFlag:
  db 80 B8 A0 00 00 00 00
//InGameVehicleStatusUI.Tick
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "garage-infinite-building-materials"
name = "Garage: Infinite Building Materials"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(BuildMats,GameAssembly.dll,8Bxxxxxx488Bxxxxxx4883xxxx5EC3E8xxxxxxxxxxE8xxxxxxxxxxxxxxxxxxxxxxxx4889xxxxxx56)
alloc(newmem,$1000,BuildMats)
//Send@FearlessRevolution|Sintrix
label(code)
label(return)

newmem:

code:
  mov eax,#99999//[rcx+rax*8+30]
  mov rbx,[rsp+38]
  jmp return

BuildMats:
  jmp newmem
  nop 4
return:
registersymbol(BuildMats)

[DISABLE]

BuildMats:
  db 8B 44 C1 30 48 8B 5C 24 38
//CampaignMiscData.GetAmountOfResource
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "stronghold-storage-max-item-amount-open-storage-tab"
name = "Stronghold Storage: Max Item Amount (Open Storage Tab)"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(StorageItemCount,GameAssembly.dll,40 8B 70 30 48 C7 44 24 28 00 00 00 00)
alloc(newmem,$1000,StorageItemCount)
//Send@Sintrix|FearlessRevolution
label(code)
label(return)

newmem:
mov [rax+30],#999999

code:
  mov esi,[rax+30]
  mov qword ptr [rsp+28],00000000
  jmp return

StorageItemCount+01:
  jmp newmem
  nop 7
return:
registersymbol(StorageItemCount)

[DISABLE]

StorageItemCount+01:
  db 8B 70 30 48 C7 44 24 28 00 00 00 00

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "grab-player-vehicle-data"
name = "→ Grab Player Vehicle Data"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanmodule(PlayerVehicle,GameAssembly.dll,48 8B 48 28 48 85 C9 0F 84 9F 00 00 00 48)
alloc(newmem,$1000,PlayerVehicle)
registersymbol(pVehicle)
label(code return pVehicle)

newmem:
mov [pVehicle],rax

code:
  mov rcx,[rax+28]
  test rcx,rcx
  jmp return

pVehicle:
dw

PlayerVehicle:
  jmp newmem
  nop 2
return:
registersymbol(PlayerVehicle)

[DISABLE]

PlayerVehicle:
  db 48 8B 48 28 48 85 C9
//FollowPlayerVehicleTransform.Tick
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "campaign-misc-data-enable-in-stronghold"
name = "→ Campaign Misc Data (Enable In Stronghold)"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]
//Send@Sintrix|FearlessRevolution
aobscanmodule(CampaignMisc,GameAssembly.dll,80 B8 A8 00 00 00 00 0F)
alloc(newmem,$1000,CampaignMisc)
registersymbol(CMDPtr)
label(code return CMDPtr)

newmem:
mov [CMDPtr],rax

code:
  cmp byte ptr [rax+000000A8],00
  jmp return

CMDPtr:
dw

CampaignMisc:
  jmp newmem
  nop 2
return:
registersymbol(CampaignMisc)

[DISABLE]

CampaignMisc:
  db 80 B8 A8 00 00 00 00
//GameBaseNotificationPipShrine.DoCheckInternal
unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "crew-member-progression-thresholds-open-crew-tab"
name = "→ Crew Member Progression Thresholds (Open Crew Tab)"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,CrewMemberProgressionThresholds.GetRecruitLevel)
define(bytes,48 89 5C 24 08)

[ENABLE]
//Send@FearlessRevolution|Sintrix
assert(address,bytes)
alloc(newmem,$1000,CrewMemberProgressionThresholds.GetRecruitLevel)
registersymbol(CrewProg)
label(code return CrewProg)

newmem:
mov [CrewProg],rcx

code:
  mov [rsp+08],rbx
  jmp return

CrewProg:
dw

address:
  jmp newmem
return:
//Send@FearlessRevolution|Sintrix
[DISABLE]

address:
  db bytes

  unregistersymbol(*)
  dealloc(*)"""
