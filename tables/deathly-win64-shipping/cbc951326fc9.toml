[game]
name = "Deathly Dominion"
exe = "deathly-win64-shipping.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=34089"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "infinite-health"
name = "Infinite Health"
category = "player"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,"deathly-Win64-Shipping.exe"+39C483B)
define(bytes,48 8B 01 4C 8D 47 18)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,"deathly-Win64-Shipping.exe"+39C483B)

label(code)
label(return)

newmem:
push rcx
mov rcx,[rcx+2D0]
xorpd xmm15,xmm15
movsd xmm15,[hp]
movsd [rcx+1F48],xmm15
jmp code

code:
  pop rcx
  mov rax,[rcx]
  lea r8,[rdi+18]
  jmp return

hp:
dq (double)33

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes

dealloc(*)"""

[[cheat]]
id = "infinite-ammo"
name = "Infinite Ammo"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix

define(address,"deathly-Win64-Shipping.exe"+3BE5BC6)
define(bytes,48 8B 01 48 8B D9)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,"deathly-Win64-Shipping.exe"+3BE5BC6)

label(code)
label(return)

newmem:
push rcx
mov rcx,[rcx+2D0]
mov rcx,[rcx+6E8]
mov [rcx+4C0],#30

code:
  pop rcx
  mov rax,[rcx]
  mov rbx,rcx
  jmp return

address:
  jmp newmem
  nop
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "super-damage"
name = "Super Damage"
category = "combat"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,"deathly-Win64-Shipping.exe"+385331C)
define(bytes,49 8B 07 4C 8D 8D A0 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,"deathly-Win64-Shipping.exe"+385331C)

label(code)
label(return)

newmem:
push r15
mov r15,[r15+2D0]
mov r15,[r15+6E8]
xorpd xmm8,xmm8
movsd xmm8,[dmg]
movsd [r15+410],xmm8

code:
  pop r15
  mov rax,[r15]
  lea r9,[rbp+000001A0]
  jmp return

dmg:
dq (double)99999

address:
  jmp newmem
  nop 5
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "super-fire-rate"
name = "Super Fire Rate"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix

define(address,"deathly-Win64-Shipping.exe"+38535F0)
define(bytes,49 8B 07 48 8D 95 60 01 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,"deathly-Win64-Shipping.exe"+38535F0)

label(code)
label(return)

newmem:
push r15
mov r15,[r15+2D0]
mov r15,[r15+6E8]
xorpd xmm7,xmm7
movsd xmm7,[rate]
movsd [r15+420],xmm7

code:
  pop r15
  mov rax,[r15]
  lea rdx,[rbp+00000160]
  jmp return

rate:
dq (double)-1

address:
  jmp newmem
  nop 5
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "no-recoil-no-spread"
name = "No Recoil & No Spread"
category = "combat"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix

define(address,"deathly-Win64-Shipping.exe"+3E3FCE7)
define(bytes,48 8B 03 44 0F B6 C7)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,"deathly-Win64-Shipping.exe"+3E3FCE7)

label(code)
label(return)

newmem:
push rbx
mov rbx,[rbx+2D0]
mov rbx,[rbx+6E8]
xorpd xmm14,xmm14
movsd xmm14,[recspr]
movsd [rbx+470],xmm14
movsd [rbx+478],xmm14
movsd [rbx+480],xmm14
movsd [rbx+488],xmm14
movsd [rbx+490],xmm14
movsd [rbx+498],xmm14
movsd [rbx+4A0],xmm14

code:
  pop rbx
  mov rax,[rbx]
  movzx r8d,dil
  jmp return

recspr:
dq (double)0

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes


dealloc(newmem)"""

[[cheat]]
id = "infinite-jumps"
name = "Infinite Jumps"
category = "movement"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,"deathly-Win64-Shipping.exe"+36070E9)
define(bytes,8B 83 34 04 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,"deathly-Win64-Shipping.exe"+36070E9)

label(code)
label(return)

newmem:
mov [rbx+434],#100

code:
  mov eax,[rbx+00000434]
  jmp return

address:
  jmp newmem
  nop
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""
