[game]
name = "Gentlemen's Club"
exe = "gentlemens-club.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=34124"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "max-drink-slots"
name = "Max Drink Slots"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,DrinkQueue:OnDrinkSlotPurchase+1f)
define(bytes,48 8B F1 C7 45 88 00 00 00 00)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,DrinkQueue:OnDrinkSlotPurchase+1f)

label(code)
label(return)

newmem:
mov [rax+E4],#8
code:
  mov rsi,rcx
  mov [rbp-78],00000000
  jmp return

address:
  jmp newmem
  nop 5
return:

[DISABLE]

address:
  db bytes

dealloc(*)"""

[[cheat]]
id = "max-bartender-mood"
name = "Max Bartender Mood"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
define(address,EvilTwin.Game.Characters.StaffBartender:OnNewTask+10)
define(bytes,48 8B F9 48 89 55 E8)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,EvilTwin.Game.Characters.StaffBartender:OnNewTask+10)

label(code)
label(return)

newmem:
mov [rcx+AC],#100

code:
  mov rdi,rcx
  mov [rbp-18],rdx
  jmp return

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes
  // mov rdi,rcx
  // mov [rbp-18],rdx

dealloc(*)"""

[[cheat]]
id = "infinite-liqour-stock"
name = "Infinite Liqour Stock"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
[ENABLE]

aobscanregion(liquor_refill,DataManager:LiquorRefillLevel,DataManager:LiquorRefillLevel+7d,48 63 49 40 0F AF C1)
alloc(newmem,$1000,liquor_refill)

label(code)
label(return)

newmem:
mov [rcx+40],#100
code:
  movsxd  rcx,dword ptr [rcx+40]
  imul eax,ecx
  jmp return

liquor_refill:
  jmp newmem
  nop 2
return:
registersymbol(liquor_refill)

[DISABLE]

liquor_refill:
  db 48 63 49 40 0F AF C1

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "club-data-boosters"
name = "→ Club Data & Boosters"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
[ENABLE]

aobscanregion(clubdata,Club:Tick,Club:Tick+6a,48 8B F1 48 8B 46 10 48 8B D0)
alloc(newmem,$1000,clubdata)
registersymbol(gcd)
label(code return gcd)

newmem:
mov [gcd],rcx

code:
  mov rsi,rcx
  mov rax,[rsi+10]
  jmp return

gcd:
dq 0

clubdata:
  jmp newmem
  nop 2
return:
registersymbol(clubdata)

[DISABLE]

clubdata:
  db 48 8B F1 48 8B 46 10

unregistersymbol(*)
dealloc(*)"""
