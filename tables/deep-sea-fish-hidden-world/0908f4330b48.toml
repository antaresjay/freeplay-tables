[game]
name = "Deep Sea Fish Hidden World"
exe = "deep sea fish hidden world.exe"
notes = "Converted from a Cheat Engine table by ndck1. Source: https://fearlessrevolution.com/viewtopic.php?t=39730"
verified = []
author = "ndck1"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "exp-module-ctrl-numpad-2-catch-fish-to-populate-values"
name = "Exp Module (Ctrl + Numpad 2) {Catch fish to populate values}"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
// Game Executable   : Deep Sea Fish Hidden World.exe
// Author            : ndck76
// Executable Version: 6000.0.63.14585
// Game Version      : Build.23585450
// Script Date       : 2026-06-11


[ENABLE]

aobscanmodule(currentexp,GameAssembly.dll,488Bxxxx4885xx0F84xxxxxxxx80xxxxxx0F84xxxxxxxx80) //ExpModule.Recalculate
alloc(newmem,$1000,currentexp)
alloc(data,8)
alloc(currentexpbkp,7)
registersymbol(data)
registersymbol(currentexp)
registersymbol(currentexpbkp)
label(code)
label(return)

currentexpbkp:
  readmem(currentexp,7)

newmem:
  mov [data],rdi

code:
  readmem(currentexp,7)
  jmp return

currentexp:
  jmp newmem
  nop 2
return:

[DISABLE]

currentexp:
  readmem(currentexpbkp,7)

unregistersymbol(*)
dealloc(*)"""
