[game]
name = "Lost In Random: The Eternal Die Demo"
exe = "theeternaldiedemo.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=34880"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "player"
name = "→ Player"
category = "player"
description = ""
hint = ""
type = "script"
source = """
define(address,Player.Rotation)
define(bytes,48 89 5C 24 10)
//Send@Sintrix|FearlessRevolution
[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,Player.Rotation)
registersymbol(PlayerBase)
label(code return PlayerBase)

newmem:
mov [PlayerBase],rcx

code:
  mov [rsp+10],rbx
  jmp return

PlayerBase:
dw

address:
  jmp newmem
return:

[DISABLE]

address:
  db bytes

unregistersymbol(*)
dealloc(*)"""
