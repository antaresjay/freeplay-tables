[game]
name = "10,000,000"
exe = "10000000.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=33282"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "grab-player-data"
name = "Grab Player Data"
category = "player"
description = ""
hint = ""
type = "script"
source = """
//Send@FearlessRevolution|Sintrix
define(address,Player:Get+1a)
define(bytes,8B 07 8B 00 8B 40 08)

[ENABLE]


assert(address,bytes)
alloc(newmem,$1000)
registersymbol(pdata)
label(code return pdata)

newmem:
mov [pdata],eax

code:
  mov eax,[edi]
  mov eax,[eax]
  mov eax,[eax+08]
  jmp return

pdata:
dq 0

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes

dealloc(*)
unregistersymbol(*)"""
