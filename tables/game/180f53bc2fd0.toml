[game]
name = "Miner Rogue"
exe = "game.exe"
notes = "Converted from a Cheat Engine table by BabyGroot. Source: https://fearlessrevolution.com/viewtopic.php?t=36748"
verified = []
author = "BabyGroot"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "player-data"
name = "Player Data"
category = "player"
description = ""
hint = ""
type = "script"
source = """
{ Game   : game.exe
  Version: 
  Date   : 2025-10-04
  Author : BabyGroot

  This script does blah blah blah
}

define(address,PlayerManager:Update+f)
define(bytes,48 8B 46 50 48 8B C8)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000,PlayerManager:Update+f)

label(code)
label(return)

alloc(Player,8)
registersymbol(Player)

newmem:

code:
  mov [Player],rsi
  mov rax,[rsi+50]
  mov rcx,rax
  jmp return

player:
dq

address:
  jmp newmem
  nop 2
return:

[DISABLE]

address:
  db bytes
  // mov rax,[rsi+50]
  // mov rcx,rax

dealloc(newmem)
dealloc(Player)
unregistersymbol(Player)"""
