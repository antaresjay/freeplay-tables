[game]
name = "From the Depths"
exe = "fromthedepths.exe"
notes = "Converted from a Cheat Engine table by N3rveMods. Source: https://fearlessrevolution.com/viewtopic.php?t=27929"
verified = []
author = "N3rveMods"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "unlimited-materials"
name = "Unlimited Materials"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscan(resourcesbase_aob,F2 48 8B C6 83 3E 00 48 8B 80 90 03 00 00 83 38 00 48 8B 40 60 48 8B C8 83 39 00 48 8B 40 38 48) // should be unique
alloc(newmem,$1000,resourcesbase_aob)

label(code)
label(return)

newmem:

code:
 mov rax,[rax+60]
mov rcx,rax

mov rcx,[rax+38]
mov rax,[rax+38]

cmp rcx,100000
jna @f
mov rcx,[rcx+10]
cmp rcx,100000
jna @f
mov rcx,[rcx+10]
cmp rcx,100000
jna @f
mov [rcx+88],0
mov [rcx+8c],4197D783

@@:

  jmp return

resourcesbase_aob+11:
  mov rcx, newmem
  jmp rcx
  nop 2
return:
registersymbol(resourcesbase_aob)

[DISABLE]
//code from here till the end of the code will be used to disable the cheat
resourcesbase_aob+11:
  db 48 8B 40 60 48 8B C8 83 39 00 48 8B 40 38

unregistersymbol(resourcesbase_aob)
dealloc(newmem)"""
