[game]
name = "Bleeding Deities"
exe = "bleeding deities.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=34975"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "player"
name = "→ Player"
category = "player"
description = ""
hint = ""
type = "script"
source = """
define(address,PlayerCombatController:Update)
define(bytes,55 48 8B EC 48 83 EC 40)

[ENABLE]
//Send@Sintrix|FearlessRevolution
assert(address,bytes)
alloc(newmem,$1000,PlayerCombatController:Update)
registersymbol(basePtr)
label(code return basePtr)

newmem:
mov [basePtr],rcx

code:
  push rbp
  mov rbp,rsp
  sub rsp,40
  jmp return

basePtr:
dw

address:
  jmp newmem
  nop 3
return:

[DISABLE]

address:
  db bytes

  unregistersymbol(*)
   dealloc(*)"""

[[cheat]]
id = "infinite-health"
name = "Infinite Health"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

//Send@Sintrix|FearlessRevclution
aobscanregion(Health,InventoryPlayer:HealthBehaviour+28,InventoryPlayer:HealthBehaviour+78,48 63 80 D8 03 00 00 85 C0 7D)
alloc(newmem,$1000,Health)

label(code)
label(return)

newmem:
mov [rax+3D8],#6

code:
  movsxd  rax,dword ptr [rax+000003D8]
  jmp return

Health:
  jmp newmem
  nop 2
return:
registersymbol(Health)
//Send@Sintrix|FearlessRevclution
[DISABLE]

Health:
  db 48 63 80 D8 03 00 00

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "one-hit-kill"
name = "One Hit Kill"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]

aobscanregion(WepDmg,PlayerCombatController:Update+1b0,PlayerCombatController:Update+26a,F3 0F 11 AE 84 00 00 00 48 B9 40 2E 54 35 47 02 00 00 33)
alloc(newmem,$1000,WepDmg)

label(code)
label(return)

newmem:

code:
  mulss xmm5,[DMG]
  movss [rsi+00000084],xmm5
  jmp return

DMG:
dd (float)99999

WepDmg:
  jmp newmem
  nop 3
return:
registersymbol(WepDmg)

[DISABLE]

WepDmg:
  db F3 0F 11 AE 84 00 00 00

unregistersymbol(*)
dealloc(*)"""

[[cheat]]
id = "infinite-jump"
name = "Infinite Jump"
category = "player"
description = ""
hint = ""
type = "script"
source = """
[ENABLE]
//Send@Sintrix|FearlessRevclution
aobscanregion(InfJump,PlayerController2D:ApplyMovement+ec,PlayerController2D:ApplyMovement+174,0F B6 86 38 04 00 00 85 C0 0F 85 79)
alloc(newmem,$1000,InfJump)

label(code)
label(return)

newmem:

code:
  mov [rsi+438],1
  jmp return
//Send@Sintrix|FearlessRevclution
InfJump:
  jmp newmem
  nop 2
return:
registersymbol(InfJump)

[DISABLE]

InfJump:
  db 0F B6 86 38 04 00 00

unregistersymbol(*)
dealloc(*)"""
