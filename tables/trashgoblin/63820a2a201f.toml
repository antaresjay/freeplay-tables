[game]
name = "Trash Goblin"
exe = "trashgoblin.exe"
notes = "Converted from a Cheat Engine table by Send. Source: https://fearlessrevolution.com/viewtopic.php?t=32644"
verified = []
author = "Send"

[meta]
source = "community"
submitted_by = ""
checked_at = ""
checked_build = ""

[[cheat]]
id = "easy-chisel"
name = "Easy Chisel"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send
define(address,PUZZLE.BLOCK.Block:TakeDamage+a)
define(bytes,D9 47 4C D9 47 54)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)
label(code return)

newmem:

code:
  mov [edi+4C],(float)-1
  fld dword ptr [edi+4C]
  fld dword ptr [edi+54]
  jmp return

address:
  jmp newmem
  nop
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""

[[cheat]]
id = "instant-clean"
name = "Instant Clean"
category = "misc"
description = ""
hint = ""
type = "script"
source = """
//Send
define(address,INTERACTION.CLEANING.CleaningManager:StartCleaningSequence+29d)
define(bytes,D9 5E 58 D9 EE)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)
label(code return)

newmem:

code:
  mov [esi+58],(float)0
  fldz 
  jmp return

address:
  jmp newmem
return:

[DISABLE]

address:
  db bytes

dealloc(*)"""

[[cheat]]
id = "all-sales-earn-1-million-coins"
name = "All Sales Earn 1 Million Coins"
category = "resources"
description = ""
hint = ""
type = "script"
source = """
//Send@Sintrix
define(address,INTERACTION.DRAG_AND_DROP.CoinBag:OnEnter+9c)
define(bytes,8B 4F 38 8B 45 F0)

[ENABLE]

assert(address,bytes)
alloc(newmem,$1000)
label(code return)
newmem:

code:
  mov [edi+38],#1000000
  mov ecx,[edi+38]
  mov eax,[ebp-10]
  jmp return

address:
  jmp newmem
  nop
return:

[DISABLE]

address:
  db bytes


dealloc(*)"""
